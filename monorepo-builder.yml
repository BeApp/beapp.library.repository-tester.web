parameters:
  # for "merge" command
  data_to_append:
    require-dev:
      symplify/monorepo-builder: '^6.0'

  # for "split" command
  directories_to_repositories:
    packages/Core: 'git@github.com:BeApp/beapp.library.repository-tester-core.web.git'
    packages/Pageable: 'git@github.com:BeApp/beapp.library.repository-tester-pageable.web.git'
